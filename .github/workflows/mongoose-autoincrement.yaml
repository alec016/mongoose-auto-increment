name: Mongoose AutoIncrement

on:
  push:
    branches:
      - 'master'
  workflow_dispatch:
    branches:
      - 'master'
    inputs:
      release:
        description: Release
        required: true
        type: boolean
        default: false

jobs:
  test:
    runs-on: ubuntu-latest
    if: |
      !contains(github.event.head_commit.message, '[test skip]')
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 30

      - name: Setup NodeJS v20.10.0
        uses: actions/setup-node@v4
        with:
          node-version: '20.10.0'
          registry-url: 'https://registry.npmjs.org'
          scope: '@alec016'

      - name: Install Dependencies
        run: npm install

      - name: Run tests
        run: npm run test

  build:
    if: |
      !contains(github.event.head_commit.message, '[build skip]')
    needs: test
    runs-on: ubutntu-latest
    steps:
      - name: Build
        run: npm run build

  publish:
    if: |
      !contains(github.event.head_commit.message, '[publish skip]')
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Publish to NPM
        env:
          NODE_AUTH_TOKEN: ${{secrets.NPM_TOKEN}}
        run: npm publish
